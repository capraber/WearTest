package com.globant.watchface

const val BG_BUNDLE_FLAG = "bg"
const val SEC_TICK_BUNDLE_FLAG = "tick"
const val MIN_TICK_BUNDLE_FLAG = "min"
const val HR_TICK_BUNDLE_FLAG = "hrs"
const val BG_AMBIENT_BUNDLE_FLAG = "bg_ambient"
const val MIN_TICK_AMBIENT_BUNDLE_FLAG = "min_ambient"
const val HR_TICK_AMBIENT_BUNDLE_FLAG = "hrs_ambient"
